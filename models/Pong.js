const keccak256 = require('keccak256'); // 哈希用
const rlp = require('rlp');
const Config = {
    version: 0x03,
    packType: 2 // 1代表ping包，pong是2
}

module.exports = Pong;
function Pong(param) {
    
}